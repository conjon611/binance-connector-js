/**
 * Binance Spot REST API
 *
 * OpenAPI Specifications for the Binance Spot REST API
 *
 * API documents:
 * - [Github rest-api documentation file](https://github.com/binance/binance-spot-api-docs/blob/master/rest-api.md)
 * - [General API information for rest-api on website](https://developers.binance.com/docs/binance-spot-api-docs/rest-api/general-api-information)
 *
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { jest, expect, beforeEach, describe, it } from '@jest/globals';
import { ConfigurationRestAPI, type RestApiResponse } from '@binance/common';

import { UserDataStreamApi } from '../../../src/rest-api';
import { DeleteUserDataStreamRequest, PutUserDataStreamRequest } from '../../../src/rest-api';
import type { NewUserDataStreamResponse } from '../../../src/rest-api/types';

describe('UserDataStreamApi', () => {
    let client: UserDataStreamApi;
    let config: ConfigurationRestAPI;
    let mockResponse: object = {};

    beforeEach(() => {
        config = new ConfigurationRestAPI({
            apiKey: 'test-api-key',
            apiSecret: 'test-api-secret',
            basePath: '',
        });
        client = new UserDataStreamApi(config);
    });

    describe('deleteUserDataStream()', () => {
        it('should execute deleteUserDataStream() successfully with required parameters only', async () => {
            const params: DeleteUserDataStreamRequest = {
                listenKey: 'listenKey',
            };

            const spy = jest.spyOn(client, 'deleteUserDataStream').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<void>)
            );
            const response = await client.deleteUserDataStream(params);

            await expect(response.data()).resolves.toBeUndefined();
            spy.mockRestore();
        });

        it('should execute deleteUserDataStream() successfully with optional parameters', async () => {
            const params: DeleteUserDataStreamRequest = {
                listenKey: 'listenKey',
            };

            const spy = jest.spyOn(client, 'deleteUserDataStream').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<void>)
            );
            const response = await client.deleteUserDataStream(params);

            await expect(response.data()).resolves.toBeUndefined();
            spy.mockRestore();
        });

        it('should throw RequiredError when listenKey is missing', async () => {
            const _params: DeleteUserDataStreamRequest = {
                listenKey: 'listenKey',
            };
            const params = Object.assign({ ..._params });
            delete params?.listenKey;

            await expect(client.deleteUserDataStream(params)).rejects.toThrow(
                'Required parameter listenKey was null or undefined when calling deleteUserDataStream.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: DeleteUserDataStreamRequest = {
                listenKey: 'listenKey',
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'deleteUserDataStream').mockRejectedValueOnce(mockError);
            await expect(client.deleteUserDataStream(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('newUserDataStream()', () => {
        it('should execute newUserDataStream() successfully with required parameters only', async () => {
            mockResponse = {
                listenKey: 'pqia91ma19a5s61cv6a81va65sdf19v8a65a1a5s61cv6a81va65sdf19v8a65a1',
            };

            const spy = jest.spyOn(client, 'newUserDataStream').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<NewUserDataStreamResponse>)
            );
            const response = await client.newUserDataStream();
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw an error when server is returning an error', async () => {
            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'newUserDataStream').mockRejectedValueOnce(mockError);
            await expect(client.newUserDataStream()).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('putUserDataStream()', () => {
        it('should execute putUserDataStream() successfully with required parameters only', async () => {
            const params: PutUserDataStreamRequest = {
                listenKey: 'listenKey',
            };

            const spy = jest.spyOn(client, 'putUserDataStream').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<void>)
            );
            const response = await client.putUserDataStream(params);

            await expect(response.data()).resolves.toBeUndefined();
            spy.mockRestore();
        });

        it('should execute putUserDataStream() successfully with optional parameters', async () => {
            const params: PutUserDataStreamRequest = {
                listenKey: 'listenKey',
            };

            const spy = jest.spyOn(client, 'putUserDataStream').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<void>)
            );
            const response = await client.putUserDataStream(params);

            await expect(response.data()).resolves.toBeUndefined();
            spy.mockRestore();
        });

        it('should throw RequiredError when listenKey is missing', async () => {
            const _params: PutUserDataStreamRequest = {
                listenKey: 'listenKey',
            };
            const params = Object.assign({ ..._params });
            delete params?.listenKey;

            await expect(client.putUserDataStream(params)).rejects.toThrow(
                'Required parameter listenKey was null or undefined when calling putUserDataStream.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: PutUserDataStreamRequest = {
                listenKey: 'listenKey',
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'putUserDataStream').mockRejectedValueOnce(mockError);
            await expect(client.putUserDataStream(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });
});
